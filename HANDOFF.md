# 클로드 코드 이관 절차

## 0. 먼저 — 이 코드의 실제 상태

> ⚠️ **`Assets/Fishing/` 의 C# 는 기획서 v0.3 시절에 쓴 것이고, 문서는 v0.4 다.**
> 그 사이에 벤치에서 실측으로 뒤집힌 결정이 많다. 코드가 문서보다 낡았다.
>
> | | 코드(현재) | 문서(v0.4) |
> |---|---|---|
> | 리소스 | 2D 스프라이트 + UV 왜곡 셰이더 | **3D 메시 + 정점 휨** |
> | 어종 ③ | 복어 | **오징어** |
> | 선회 | 각속도를 직접 지정 | **ω = v / R_min** |
> | 접근 | 단일 상태 | **조립 가능한 6단계** |
> | School | PathType 의 하나 | **변형자** (어느 법칙에도 얹힘) |
> | 튜닝 값 | `public const` | **ScriptableObject** |
> | 무리 | 어종 속성 | **개체 속성** |
>
> **문서와 두 벤치가 정답이고, C# 은 출발점일 뿐이다.** 낡은 코드를 보존하려 들지 말 것.

---

## 1. 유니티 프로젝트

현재 저장소의 상위 `A:\A_Workspace\_Unity`는 HTML 프로토타입·문서·원본 아트를 두는
작업공간 루트이고, `FishingGame/`이 정식 Unity 프로젝트 루트다. Unity Hub와 에디터는
항상 `FishingGame/`을 대상으로 한다. 상위 루트에 별도의 Unity 프로젝트를 만들거나
두 프로젝트를 병행하지 않는다.

- **URP 템플릿(3D)** 로 새 프로젝트. 카메라는 **Orthographic**, 위에서 수직으로 내려다봄
  (v0.3 문서의 "URP 2D" 는 스프라이트 전제였다. 3D 메시를 쓰기로 했으므로 일반 URP 렌더러가 맞다)
- 해상도 가로 16:9 기준. 방향은 미결정이라 나중에 바꿔도 코어는 안 바뀜

## 2. 배치

### 2-A. 새 프로젝트인 경우

압축을 풀어 프로젝트 루트에 그대로 넣는다.

```
<UnityProject>/
  CLAUDE.md · HANDOFF.md · Docs/ · Art/ · Assets/Fishing/
```

### 2-B. 보일러플레이트가 이미 있는 프로젝트인 경우 ★

**게임을 한 폴더에 몰아넣고, `CLAUDE.md` 를 그 폴더 안에 둔다.**
Claude Code 는 루트 `CLAUDE.md` 와 **작업 중인 하위 폴더의 `CLAUDE.md` 를 함께** 읽는다.
루트는 보일러플레이트 규약, 게임 폴더는 이 게임 규칙 — 이렇게 갈라두면 서로 안 섞인다.

```
<UnityProject>/
  CLAUDE.md                      ← 기존. 보일러플레이트 규약 (건드리지 않는다)
  Assets/
    _Core/ (또는 기존 보일러플레이트 위치)
    Games/Fishing/
      CLAUDE.md                  ← 이 묶음의 CLAUDE.md 를 여기로
      HANDOFF.md
      Docs/                      기획서 · 아트가이드 · 벤치 2종
      Scripts/                   Assets/Fishing/Scripts 의 내용
      Shaders/  Art/
  Tools/Fishgen/                 ← Art/fishgen.py, stat.py (Assets 밖)
```

루트 `CLAUDE.md` 에 **한 줄만** 추가한다:

```
- 낚시 게임 작업은 `Assets/Games/Fishing/CLAUDE.md` 를 함께 읽을 것.
  그 폴더 안의 규칙이 루트 규약보다 우선한다.
```

### 2-1. 경계 — 무엇이 보일러플레이트고 무엇이 게임인가

판별 기준 하나면 충분하다.

> **다음 게임(예: 곤충채집 게임)이 이 파일을 고치지 않고 쓸 수 있는가?**
> 아니면 그건 보일러플레이트가 아니라 이 게임의 코드다.

| 보일러플레이트 | 이 게임 |
|---|---|
| 세션 타이머 · 씬 전환 · 저장/로드 | 어종 · 궤도 법칙 · 접근 방식 · 찌 |
| 오디오 · UI 프레임워크 · 입력 | 기획서 §1~§8 의 규칙 전부 |
| 튜닝 SO **기반 구조** (`[Range]`, 플레이 중 편집, JSON 덤프) | 튜닝 **값** (`FishingTuning`) |
| 오브젝트 풀 · 팝업 · 로컬라이즈 | 상태 머신 · 유인 · 재착수 페널티 |

**asmdef 로 방향을 강제한다.** 보일러플레이트 asmdef 는 게임 asmdef 를 **참조하지 않는다.**
게임만 보일러플레이트를 참조한다. 그러면 "게임 로직이 보일러플레이트로 샜는가" 가
코드 리뷰 항목이 아니라 **컴파일 에러**가 된다.

**시뮬레이션 계층은 보일러플레이트의 존재를 몰라야 한다.**
`Fish` · `PathEvaluator` · 접근 방식 · `Bobber` 안에서 `XxxManager.Instance` 를 부르지 말 것.
데이터를 받아 데이터를 돌려주는 형태를 유지한다. 그래야:

- 헤드리스로 밸런스를 돌릴 수 있다 (이 프로젝트 버그의 절반이 그렇게 잡혔다)
- 기획서 §9 의 "결정론 = 자동낚시·리플레이 공짜" 가 성립한다
- 보일러플레이트를 갈아끼워도 게임이 안 깨진다

### 2-2. 이미 추출된 보일러플레이트를 쓰는 경우

이전 프로젝트에서 **기계적으로 추출한** 보일러플레이트라면, 예측이 아니라 추출이므로
출발점은 옳다. 다만 **1회 추출은 검증이 아니라 가설**이다. 고유한 실패 모드가 셋 있다.

| 실패 모드 | 증상 |
|---|---|
| **이름은 일반적, 가정은 특정적** | `StageManager` 인데 "스테이지엔 웨이브가 있다", "실패 조건이 있다" 를 전제 |
| **결합을 옮겼을 뿐 방향은 재검토 안 됨** | 게임플레이 안에서 `XxxManager.Instance` 를 부르는 구조가 그대로 따라옴 |
| **"왜" 가 이전 프로젝트에 남음** | 동작은 하는데 어디가 하중을 받는지 아무도 모름 → 맹신하게 됨 |

**이 게임은 그 가설의 첫 검증이다.** 이 게임엔 흔치 않은 성질이 여럿 있어서 가정을 잘 건드린다:

- **실패 조건이 없다** (타이머 라운드, 죽지 않음)
- **코어 루프가 입력이 아니라 관찰이다** — 상시 입력을 전제한 튜토리얼·입력 매니저와 부딪힌다
- **결정론이 요구된다** — 헤드리스 밸런스 검증(이 프로젝트 버그의 절반)이 여기 걸려 있다
- **점수를 저장하지 않는다** (인벤토리로 계산) — 점수 중심 세이브와 어긋난다

#### asmdef 를 붙이는 것 자체가 감사다

보일러플레이트 asmdef 가 게임 asmdef 를 참조하지 못하게 걸어라.
**컴파일되면 추출이 깨끗했던 것이고, 안 되면 그 에러 목록이 곧 누수 목록이다.**
따로 코드를 읽을 필요가 없다.

#### 고칠 것과 적어둘 것을 나눈다

| | 지금 |
|---|---|
| **버그 · 누락 기능** | 바로 고친다 |
| **구조 · API 모양 변경** | **적어두고 이 게임이 끝난 뒤에** |

구조를 게임 만드는 중에 바꾸면 **측정 대상을 측정 중에 바꾸는 것**이 된다.
그리고 게임 1.5개짜리 정보로 하는 구조 변경은 여전히 추측이다.

**마찰 로그를 남긴다.** 아래 셋은 전부 "추출이 게임 1에 물들어 있었다"는 신호다.

- 보일러플레이트 때문에 **어댑터를 쓰게 될 때**
- 호출하지 않고 **복붙하게 될 때** (재사용 가능하지 않았다는 뜻)
- 매니저가 가로막아 **헤드리스로 테스트할 수 없을 때** (의존 방향이 틀렸다는 뜻)

이 게임이 끝나면 그 로그가 **두 번째 추출**의 근거가 된다. 그게 진짜 보일러플레이트가 되는 지점이다.

**"왜" 는 역으로 채운다.** 이전 프로젝트에 남은 이유를 지금 복원하되,
**이번에 실제로 건드린 부분만.** 안 건드린 코드까지 추측으로 문서화하면 그게 또 화물 숭배다.

**버전을 붙인다.** 복사 폴더가 아니라 패키지로 (로컬 UPM 또는 별도 asmdef + 버전).
게임 A 가 v1 에 고정돼 있는데 게임 B 용 v2 가 조용히 A 를 깨는 걸 막는다.

## 3. 첫 프롬프트

Claude Code 를 프로젝트 루트에서 실행하고 아래를 그대로 붙여넣는다.
**바로 고치라고 시키지 않는 게 핵심이다.** 먼저 격차를 파악시킨다.

```
이 프로젝트는 모바일 방치형 낚시 게임이야. 구현을 이어서 할 건데, 시작 전에 상태 파악부터 하자.

먼저 읽어줘:
- CLAUDE.md (특히 "절대 임의로 바꾸지 말 것" 표)
- Docs/01_기획서_v0.4.md 의 §0, §4, §5-3, §5-6, §11
- Docs/02_아트리소스가이드_v2.0.md

중요한 전제: Assets/Fishing 의 C# 는 기획서 v0.3 시절 코드고, 문서는 v0.4 야.
그 사이에 프로토타입 실측으로 뒤집힌 결정이 많아서 코드가 문서보다 낡았어.
HANDOFF.md §0 에 차이가 표로 정리돼 있어.

첫 작업은 코드 수정이 아니라 **감사(audit)** 야. 다음을 표로 만들어줘:

1. Assets/Fishing 의 각 파일이 v0.4 문서와 어긋나는 지점
2. 각 항목을 "그대로 씀 / 고침 / 버리고 새로 씀" 중 무엇으로 볼지와 그 이유
3. 컴파일부터 통과시켜야 하는 것과, 어차피 다시 쓸 거라 지금 고칠 필요 없는 것의 구분

아직 아무것도 고치지 마. 표만 보고 순서를 같이 정하자.

참고: 이 스크립트들은 유니티 없이 작성돼서 컴파일 검증이 안 됐어. 에러가 있는 게 정상이야.
```

**보일러플레이트가 있는 프로젝트라면 아래를 덧붙인다:**

```
이 프로젝트엔 이미 보일러플레이트(매니저 계층)가 있어. 앞으로 비슷한 게임을 여러 개 만들면서
같이 갱신할 거야. 그래서 감사에 두 항목을 더 넣어줘:

4. 보일러플레이트가 이미 제공하는 것과 Assets/Games/Fishing 이 중복 구현한 것
   (세션 타이머, 저장, 오디오, 오브젝트 풀, 입력 …)
5. 반대로, 게임 고유 규칙인데 보일러플레이트로 올리고 싶어질 만한 것 —
   **올리면 안 되는 이유와 함께** 표시해줘

판별 기준은 하나야: "다음 게임이 이 파일을 고치지 않고 쓸 수 있는가."
아니면 보일러플레이트가 아니라 이 게임 코드야. HANDOFF.md §2-1 참조.

6. 이 보일러플레이트는 이전 프로젝트에서 기계적으로 추출한 거야. 그래서 이름은 일반적인데
   가정은 이전 게임에 물들어 있을 수 있어. 이 게임은 이런 성질이 있는데 —
   실패 조건 없음 / 타이머 라운드 / 코어 루프가 입력이 아니라 관찰 / 결정론 필요 /
   점수를 저장하지 않고 인벤토리로 계산 — 보일러플레이트의 어떤 가정과 부딪히는지 짚어줘.

그리고 asmdef 를 확인해줘. 보일러플레이트 asmdef 가 게임 asmdef 를 참조하면 안 돼.
아직 asmdef 가 없다면 추가하는 것 자체가 감사가 돼 — 컴파일 에러 목록이 곧 누수 목록이야.
```

## 4. 그다음 순서 (감사 결과를 보고 조정)

**① 컴파일 통과** — 로직은 건드리지 말고 빌드만 통과시킨다.
어차피 다시 쓸 파일은 최소 수정만.

```
감사 표에서 "그대로 씀 / 고침" 으로 분류한 파일만 대상으로,
로직은 바꾸지 말고 컴파일 에러만 잡아줘. 콘솔 에러를 붙여넣을게.
"버리고 새로 씀" 파일은 컴파일만 되게 최소 수정만 해줘.
```

**② `FishingTuning` 을 `const` → ScriptableObject**
`const` 는 컴파일 시점에 박히는 값이라 플레이 중 튜닝이 구조적으로 불가능하다.
CLAUDE.md 의 해당 절 참조. **느낌 값과 규칙 값을 갈라서** 만들 것.

**③ 생성기를 C# 로 이식** (`Art/fishgen.py` → 에디터 툴)
순수 수학 150줄이고 파이썬·JS 두 벌이 이미 있다. 옮기면 Blender 의존성이 사라지고
"파라미터 수정 → 메뉴 클릭 → 즉시 재생" 이 된다.

```
Art/fishgen.py 를 유니티 에디터 툴로 이식해줘.
- 어종 파라미터는 ScriptableObject 로 (fishgen.py 맨 위 SPECIES 딕셔너리가 원본)
- 메뉴에서 실행하면 Mesh 애셋을 생성/갱신
- 정점 컬러 포함, UV·텍스처·본 없음
- Docs/02_아트리소스가이드_v2.0.md §2 의 규격을 그대로 지킬 것
  (특히 §2-3 지느러미 밑동, §2-4 눈, +X 가 앞 방향)
같은 로직이 Docs/turn-bench.html 안에 JS 로도 있으니 대조용으로 써.
```

**④ 이동·접근을 v0.4 로** — §5-3(ω = v/R), §5-6(접근 조립), §4-1(법칙+변형자)

**⑤ 휨 셰이더** — 아트가이드 §2-6 의 계약대로. 기존 `FishWave.shader`(UV 왜곡)는 버린다

## 5. 작업 지시할 때 붙일 말

> **"CLAUDE.md 의 '절대 바꾸지 말 것' 표를 지켜줘."**

이 프로젝트엔 버그처럼 보이지만 의도된 규칙이 20개 넘게 있다. 각각 이유가 문서에 있다.

**기획을 실제로 바꾸는 작업이라면 `Docs/01_기획서_v0.4.md` 도 같이 고치라고** 지시할 것.
안 그러면 문서와 코드가 또 갈라진다 — 이번에 이미 한 번 갈라졌다.

## 6. 검증

**이 게임의 재미는 정지 상태로 검증되지 않는다.** 이번 작업에서 나온 버그 10개 중 7개가
스크린샷·코드 리뷰로는 정상으로 보이는 종류였다 (꼬리가 앞장서서 헤엄침, 게걸음,
소용돌이, 붙어 있는데 떨어져 보이는 지느러미…).

| 확인할 것 | 방법 |
|---|---|
| 이동이 자연스러운가 | 재생하고 **손 떼고 30초**. 제자리 회전·게걸음·소용돌이가 보이면 §5-3 위반 |
| 관심 큐가 읽히는가 | 찌 던지고 20초 관찰. 다가오는 놈과 배회하는 놈이 구분되는가 |
| 밸런스 | 90초 세션 목표 **10~13마리, 40점 근처** |
| 형태 | **게임 실제 크기(100px 내외)** 로 볼 것. 확대해서 판단하면 놓친다 |

두 벤치를 브라우저로 열면 **정답 동작**을 볼 수 있다. 유니티 결과가 이상하면 벤치와 비교할 것.
